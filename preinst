#!/bin/sh
# preinst script for lubuntu-lark-neuroshell

set -e

if [ "$(id -u)" -ne 0 ]; then
   echo "This package must be installed as root." >&2
   exit 1
fi

echo "Starting pre-installation checks for Lubuntu Lark NeuroShell..."

echo "Pre-installation checks passed."

exit 0